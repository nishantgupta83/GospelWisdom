<?xml version="1.0" encoding="utf-8"?>
<!-- android_app/app/src/main/res/values-night/themes.xml -->
<resources>
    <!-- Dark Theme -->
    <style name="Theme.GospelWisdom" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <!-- Splash Screen Theme (Dark) -->
    <style name="Theme.GospelWisdom.Splash" parent="Theme.GospelWisdom">
        <item name="android:windowBackground">@color/splash_background</item>
    </style>
</resources>
